import {AppRegistry} from 'react-native';
import ToDoList from './PPMSesi3/ToDoList'; // Impor komponen TodoList
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ToDoList);
